# Final Project COP1500
# Trignomtric Function Plotter

import math
import mathplotlib

# trig function calculator

def trigSolver(a,b,c,d):
    # variables

   a =
   b =
   c =
   d =

    # print possible functions
    print("Possible functions include")
    print("COS,SINE,TAN")

    prompt = input("Enter the Desired Function")

    if prompt == COS:
            a = int(input("Enter the amplitude")
            b = int(input("Enter the period"))
            phaseShift = input("is there phaseshift?")
            if phaseShift == True:
                c = int(input("Enter the phaseshift"))
            else:
                